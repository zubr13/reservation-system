{
  "name": "reservation-system",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "cd server && nodemon register.js",
    "start":
      "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "babel-eslint": "^8.2.3",
    "concurrently": "^3.5.0"
  }
}
